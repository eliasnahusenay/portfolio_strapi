const Hero = ({ title }) => {
    return (
      <div className="h-60 bg-blue-500 flex items-center justify-center text-white text-4xl font-bold">
        {title}
      </div>
    );
  };

  export default Hero;
